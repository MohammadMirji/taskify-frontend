import React from "react";

const Loader = () => {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: "100px",
        fontSize: "18px",
        color: "#555",
      }}
    >
      Loading...
    </div>
  );
};

export default Loader;
